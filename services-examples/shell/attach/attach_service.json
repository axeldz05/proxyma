{
    "type": "bidi_stream",
    "exec": "uv run --project __SERVICES_DIR__ python __SERVICES_DIR__/shell/attach/main.py",
    "schema": {
        "name": "shell.attach",
        "type": "bidi_stream",
        "description": "Lab remote shell (allowlisted builtins only — no real PTY exec)",
        "parameters": {
            "session_id": {
                "type": "string",
                "required": false
            }
        },
        "outputs": {
            "status": {"type": "string"}
        }
    }
}
