{
    "type": "bidi_stream",
    "exec": "uv run --project __SERVICES_DIR__ python __SERVICES_DIR__/clipboard/main.py",
    "schema": {
        "name": "clipboard.sync",
        "type": "bidi_stream",
        "description": "Lab clipboard sync over bidi NDJSON (set/get with data_b64)",
        "parameters": {
            "session_id": {
                "type": "string",
                "required": false
            }
        },
        "outputs": {
            "status": {"type": "string"}
        }
    }
}
