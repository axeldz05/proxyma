- ~~Validaciones por hacer:~~
    - ~~Hay que implementar una validacion cuando ya se tiene en claro como seran los mensajes.~~
    - ~~Cuando un nodo recibe peticion para unirse al cluster, tiene que verificar que el address sea accedible.~~
    - ~~Announce el endpoint /peers/announce se deja expuesto frente a peticiones invalidas~~
- ~~Cambiar la forma de hacer Sync con HTTP a usar sockets en sistemas unix. Eliminar el "/sync" endpoint~~
- ~~Inconsistencia y acoplamiento circular en `P2PRoundTripper`: Rediseñar para usar un modelo push-based en lugar de callbacks al Server. Centralizar logs de red con `slog`.~~
- ~~Seguridad y bugs en Relay Fallback: Validar mTLS CommonName en `/relay/poll` para evitar spoofing; prevenir fugas de canales de relay en memoria limitándolos a peers conocidos; resolver fallos de TLS handshake en polling y respuestas usando el transporte mTLS del `PeerClient`; usar ReqID criptográficamente seguro y optimizar el multiplexor.~~

Cambiar el BuildGRPCHandler para aceptar tres tipos de conexiones (Stubs creados, falta implementacion):
1. Streaming Bidireccional (gRPC Bidi o WebSockets). Considerar implementar algoritmos de resolución de conflictos como CRDTs (Conflict-free Replicated Data Types) u OT (Operational Transformation).
2. Server-Streaming (gRPC Server Stream o HTTP Range Requests). Un handler que devuelva un io.Reader (o un canal <-chan []byte) en lugar de un map[string]any.
3. Caso donde no se usa ni gRPC ni HTTP: WebRTC (UDP). Para el input considerar usar WebSockets o canales de datos de WebRTC (DataChannels).

Próximas características y problemas de red (según README.md):
- ~~Implementación de NAT Traversal completo usando STUN/TURN Hole Punching (o AutoNAT con libp2p) para permitir conexiones directas y evitar el tráfico redundante a través del Sponsor de Relay.~~
- ~~Refactorización y simplificación de código: Reemplazar Mutex en cmd/proxyma-bind con getSrv() RWMutex, aplanar ifs anidados en RoundTrip y ExecuteSync mediante helpers dedicados, y limpiar advertencias del linter golangci-lint.~~
- Migración de BoltDB a BadgerDB (LSM-based/WiscKey) para optimizar el I/O en SSDs y manejar grandes conjuntos de metadatos del VFS de forma eficiente.
- Consistencia Distribuida: Integrar un modelo de consenso distribuido (como Raft) para la unificación del estado global de la red.
- Reducción del overhead de serialización: Adoptar gRPC (y grpc-gateway) para comunicación entre nodos y streaming, reemplazando el protocolo JSON actual.
- Buscar la posibilidad de permitir subscribcion de servicios. Posiblemente relacionado con refactorear a una arquitectura estilo microkernel centralizado en servicios.